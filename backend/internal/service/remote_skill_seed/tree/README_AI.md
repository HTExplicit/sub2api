# Security Research Skills: Cloud Loader Compatibility

This file is served from a remote Skills mirror. It is a compatibility entry
for clients whose bootstrap still requests `README_AI.md`; it is not a local
installation guide.

```text
REMOTE_ROOT = https://moxinggang.com/skills/security-research/current
```

Do not run local tool-index refresh or bootstrap scripts, write global client
configuration, detect a local package root, or follow clone-only paths. The
canonical skill entry is self-contained and defines its own remote path
contract.

Read `REMOTE_ROOT/SKILL.md` now. Follow its progressive loading rules and use
`REMOTE_ROOT/<path>` for every supporting file. In particular, do not request
`skills/routing.md`, `skills/tool-index.md`, or other relative paths from this
compatibility page.
