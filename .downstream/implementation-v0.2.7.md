# v0.2.7 实施状态

本分支尚未发布。按用户最新批准的“必要修复与加速上线”计划交付：原始额度/费用/限流与推理强度、官方 0.2.7 整合、已实现的七域插件/独立界面/单插件更新全部保留；仅消除影响本次构建、启动、权限、持久化、计费、插件更新与部署的已知阻塞。全项目历史审查全部关闭不再是上线前置条件，未完成项继续如实保留 pending。

## 固定基线与授权

- 生产源码：`65a82d9a8f079f389f08e092d318a663123095e9`；官方 `v0.2.7`：`aea725f2ea644d5592d0bbb1d63b607efa7e200a`。
- 工作目录：`work/sub2api-v027-plugins`；分支：`feat/v027-plugin-architecture`；官方合并提交：`5a4b26a1d`；前序基础契约提交：`27e6d8f17`。当前未提交修改均属于本任务，必须保留。
- 插件域：Codex 运行扩展、Cindy Provider、账号工具、提示词/技能、模型目录/策略、图像工具、界面/观测。插件独立模块，仅引用公开 SDK；不能把空壳、开关或调用旧宿主业务算作迁移。
- 宿主保留认证授权、账号基础数据、凭据刷新、事务/计费、通用调度与流式生命周期、通用正确性修复。
- 已确认业务界面也须随插件独立发布；本次交付单插件更新。独立更新后固定版本，宿主升级保留，明确选择“跟随内置版”才恢复随镜像更新；不兼容时保留数据并显示不可用。
- Cindy 创建分组规则已确认：保留现有直接 API 自动绑定默认组的行为，在最终有效分组仍为空时拒绝新建；不重写既有账号或分组关系。此决定替代创建声明草案中“必须显式选择分组、禁止默认组回退”的提议。
- 普通 OpenAI 新建账号不再因 Laxa 地址自动切为强制 Responses；保持官方普通 OpenAI 语义，保留明确选择的协议及所有既有身份、地址和模型映射。Cindy 创建字段/默认值仍按独立声明迁移，不把两类身份混为一谈。
- 提示词/技能管理界面已确认跟随站点语言；英文环境使用英文，替代此前固定中文约定。此调整仅涉及界面文案，提示词正文、版本和发布决定不随语言切换改变。
- 编辑保存已按真实账号身份实现：普通 OpenAI+Laxa 保留普通 OpenAI 的显式透传/协议设置；未改字段保留原状（含缺失），明确 `auto` 保存为 `auto`，明确清除才删注册键，不因只改名称/备注而回填 Cindy Responses 默认。不批量重写既有账号；`account.edit.v1` 和复制保留语义已在 `e36c11550` 收口。
- 复制 Cindy 账号已确认保留原协议设置及缺失状态，不再对副本补写 Responses 默认；其余分组/优先级、代理选择、新设备 ID 与初始调度状态沿用现有复制规则，不套入新建默认、最低分组或 ID-less 准入规则。
- 插件停用撤下贡献入口并停止能力；启用但故障保留入口并显示不可用原因，后端同样执行。数据、已停止续期状态及持久账本保留。
- 部署目标为当前具名入口对应的源站 VM `2166044 / calm-frogs-1`，不根据旧地域文字切换主机。保留现有网络/资源/映射、Image Studio 与下游 Responses 图像桥接关闭、公开模型管理退役及 CPA 停机。使用固定 digest、自然排空、`runtime=preserve` 的 image-only 路径，不恢复已取消保护，不访问香港。

## 本次交付清单与排除范围

- [x] 保留原始额度与费用修复、普通/Spark 范围、显式推理强度、官方 0.2.7，以及七域插件、独立界面、目标插件进程更新和固定版本持久化实现。
- [x] 账号创建、编辑、复制合同已收口；已有同摘要的业务、PostgreSQL 和模拟 API 浏览器证据直接复用。浏览器结果不等于生产权限或真实模型质量验收。
- [ ] 收口网关既有失败及仍影响交付的已登记阻塞，精确提交并通过正常 PR 必需检查、合并。
- [ ] 从最终合并源码发布 `v0.2.7-codexrip.1`；沿用 `codexrip-plugins-v1` 正式发布者，七个签名包、lock、来源证明和镜像同源，镜像复用同一批正式包，不使用旧 `bundle-seven` 或重生成发布身份。
- [ ] 现场核摘要/权限后，仅通过固定包更新四个已审维护脚本，再更新部署器、包装器及共享库的五文件发布集合；其余受保护文件继续做一致性检查，不新增服务/timer。
- [ ] 仅重建目标 Sub2API 容器，取得规定公网健康结果，在根项目生产事实与最小交付证据中记录最终源、digest、插件版本和部署状态。

历史诊断、旧完整验收、额外统计采集与容量调整工具的未完成加固全部后置。根 `artifacts/evidence/v027-assist-scope-handoff.md` 和各 WIP 报告保留原始红/绿/未运行事实，草稿不上传、不安装、不执行，不纳入本次维护包。正常 image-only 部署不调用旧完整验收，不增加 canary、模型请求、长观察、业务备份或自动数据回滚。

VPN 的新六出口和日本出口退役属于用户确认完成的外部任务成果，原样保留。本任务不再修改其源码、配置、测试或重做验收，也不把外部成果记成本任务的验证通过。

## 已实现的工作

- 账号专页/筛选布局已接入 `account.view.v1`；Cindy 声明和独立控件保留旧入口，宿主逐调用校验范围、分页前过滤及独立双 owner 执行来源，任务历史/取消由宿主保留。新增 SQL 锁、probe 原子收尾及 Resume CAS 已有最小本地 PostgreSQL 补证；未把 DOM 测试当浏览器目视验收。
- `account.create.v1` 已接入公开 Go/TS/JSON 声明和原生表单。canonical Cindy 在中央创建边界执行当前声明及 ID-less Provider/Admin 双 100% 准入，默认组回退后才检查最小组，实际 Ent 事务锁和外层作业 lease 保留到提交/回滚；普通 OpenAI+Laxa 不隐式改协议。合法导入映射、无 tag 数值语义和显式值保留，Create 独立测试及真实 PG 补证已通过。
- `account.edit.v1` 使用同一 native 行的状态摘要与版本前置条件，未改字段、显式 `auto`、有限 clear 及复制缺失状态均保留。后端定向结果含真实 PostgreSQL，前端类型、定向交互与语言检查已有证据；创建/编辑离线浏览器的 DOM 与实际目视记录在根 `artifacts/evidence/v027-account-create-edit-browser-review.json`，全部 API 为模拟且无真实模型调用。
- 官方 0.2.7 已合并。上游新能力与下游冲突决议沿用合并提交。
- 额度统一读模型：保留真实 5h/7d/30d，窗口各自的观测时间与绝对期限；完整查询清除已撤销窗口，普通与 Spark 查询使用同一转换。旧字段仅兼容。已知未来期限不会因样本变旧而被推迟或解除。
- 硬额度错误由额度状态独立拥有，未知期限保持未知，不写成普通 429 或运行时冷却；恢复证据不会在之后的窗口到期时重新激活旧错误。普通 429、管理员禁用与其他错误独立保留。
- 快照数据库写入对账号行加锁后比较采样时间，修复并发旧观察覆盖新观察；首次更新为未变更的旧窗口保留原观测时刻。
- 费用校准按配对的费用增量/使用率增量持久化；观测跨 HTTP/WS 请求、异步账单提交及丢弃路径，Redis epoch/gap 使不可归因观察失效。Spark 采用自己的窗口及父账号的主体/计费倍率。费用历史缩减也重新建立基线。
- 单个和逐项批量连接测试保存 reasoning_effort。出站 Responses/Chat/Grok/OpenCode Go 已接入；不支持序列化该参数的协议不提供选项、不静默忽略。批量初始记录及重试保留各账号选择。
- 通用扩展 SDK、独立进程 RPC、CAS/执行租约、同事务调度摘要、后台任务、贡献注册和 iframe UI 已实现。
- Codex 插件实际拥有 292 采集、验证、注入、有限续期、代理诊断。宿主旧采集循环、票据内存缓存、旧续期数据库执行器及旧全局设置读写已移除；旧数据只供一次迁移和保护性读适配。
- 取消与配置切换会丢弃迟到票据；中断的首次采集在执行租约过期后转为已中断，不重新请求。有效旧票跳过、停用/错误账号支持、两个有限续期阶段不变。
- 插件设置为唯一配置写入来源，旧全局票据写入返回明确迁移提示，不暴露遗留代理凭据。账号页保留原操作位置，跨页资格采用通用账号元数据查询。
- 签名打包命令、镜像 bundle 引导、持久迁移账本由前序实现保留。CI 已新增独立插件模块和签名进程检查，删除测试的引用改为迁移后的等价入口，未取消必需检查。
- 模型目录静态数据和供应商/产品范围匹配已移入独立 `plugins/model-policy` 模块，公开 SDK 传输容量参考。宿主保留上游观测、自定义值和来源优先级等正确性规则，目录通过通用能力接口读取；有界缓存按配置版本隔离，每次先核验插件启停与健康。无内置目录暗中回退。原静态完整性、精确别名/原始键和容量参考回归通过。
- 两个独立程序及签名 bundle 的本地验证通过：`TestExtensionRuntimeUsesOfficialProcessAndHostBroker`、`TestCatalogExtensionRuntimeUsesIndependentProcess`、`TestFirstPartyBundleContainsMatchingSignedDomainPackages`。构建目录为项目根 `artifacts/tmp/v027-plugin-check-0a4b635d31844b1f93e04e44b0513c47`，仅离线程序和合成数据；私钥为该目录内新生成的临时测试签名密钥，不可提交，任务完成后清理。
- `plugins/prompt-skills` 已接管提示词应用范围、内容完整性、协议载体、回显、模板名称/编排/受管来源策略，以及默认模板和远程 GitHub 来源验证。宿主只执行请求改写与持久事务；客户历史不进入 RPC，超过 4 MiB 的历史完整保留，重试使用已验证的原始快照。
- 技能目录、458 个固定文件及提示词原文已移入插件。模块校验固定清单、来源、完整引用图和文件分类/改写计划，宿主按有界块读取并核对 SHA-256；约7.2 MB数据不会合并成一条RPC。原有文件字节与发布提示词固定哈希不变，LF属性、维护脚本和CI路径已同步。
- 提示词数据初始化改为在插件就绪后进行，支持启动时停用、之后启用；停用保留数据并停止注入，启用但故障仍对已配置提示词明确报不可用。同步任务绑定插件上下文，迟到结果不写入；失败状态用独立短事务收尾。来源校验修复了归档摘要长度缺失，空串或不足64位的SHA-256不再通过。
- 已移除启动时删除旧技能目录、同步历史及候选记录的调用和实现。公开技能内容先检查插件状态，关闭返回404、故障返回503；可变的 current URL 使用重新验证缓存，避免发布后继续使用旧缓存。管理页面跟随贡献启停。
- `plugins/account-tools` 已接管分类名称、排序、赋值、批量分类和推理选择策略，以及逐账号批量测试计划。旧 HTTP 接口成为适配入口，数据库事务/关系约束及测试出站协议仍由宿主执行。负数标签 ID 不再被静默丢弃。
- 插件表单和宿主表面由 manifest 贡献声明。推理选择停用后撤下；故障时保留选择但阻止提交；分类及批量测试入口随贡献启停。失效状态传入 Teleport 弹窗，关闭按钮、宿主持久任务进度与取消能力仍保留。
- 账号导入完整界面、统一设置及 JSON 解析 Worker 已迁入 account-tools。宿主原入口只挂载插件并投影必要的分组/代理选项，既有代理账密不进入界面上下文；用户明确选择的导入文件仍由插件界面解析，经具名导入资源提交。Worker 随签名 UI 内联，只允许 Blob Worker，直连网络与原生表单提交仍被 CSP 拒绝。预览/提交要求覆盖全部账号的管理员能力绑定，任务记录承接宿主确认的插件归属，不能换成另一插件。
- SDK 高度计算现覆盖 Teleport 弹窗的自然内容和滚动区域，避免空根节点导致 iframe 收缩、底部按钮不可点击；宿主任务进度与历史仍由通用操作窗口呈现。
- 通用账号任务保存插件归属和执行代次，创建事务以当前安装记录加锁验证；失败重试保留原归属并明确准入当前版本，收窄或停用的范围不能被重试扩大。宿主长任务、具名资源及领域 IO 使用独立共享会话租约，更新须等待实际 cleanup，不能只杀旧插件进程就抢先切换。每项执行前重查实际目标，管理动作的提交与执行也使用同一账号/灰度判断。
- 导入创建/更新/拒绝与重复凭据、设备冲突策略已迁入 account-tools；宿主只提取校验事实、摘要和最多两个歧义见证 ID，不向策略进程传凭据、设备原文、名称或代理信息。大批导入分块准入，再汇总插件选择的引用并完成跨块冲突决策，保持官方4 MiB RPC边界；宿主验证目标身份、显式分组与结果索引。旧导入决策执行路径已删除，提交与执行时预览共用同一插件入口。
- 升级前未记录插件归属的待执行导入/分类/批量测试任务，在新执行路径中同样绑定当前插件生命周期，历史记录不重写；停用时将取消落入宿主任务记录，正常完成的 cleanup 不会误标取消。
- 提示词域停用/故障会停止后台订阅、同步运行上下文和新工作入口，快照、已发布技能和持久历史保留；恢复后可再次启动。停止与同步任务创建在同一运行锁下检查，停用后不再先创建数据库任务再失败；健康启动只加载一次提示词状态。
- 提示词/技能发布和回滚准入已进入插件，保持任意选中非当前版本可回滚的既有行为。宿主先读取与expectedRevision一致的持久快照，后续事务再次CAS；插件不能把不可变目标版本的编排替换为另一个bundle。repository不再重复调用发布来源/编排业务策略，继续独立校验正文、摘要、结构和事务完整性。
- 技能来源profile、文件限额及具名存储布局计划已进入插件；宿主只允许已有注册来源/公开根、哈希候选键和受限槽位，保留固定根内IO、软链接/摘要检查、暂存与原子替换。数量/声明字节限额在下载前生效，发布数据也受限额约束；旧生产布局函数已移出执行路径，测试仍覆盖既有目录格式。
- 命名操作按能力与平台/类型分派，重叠启用声明被拒绝；操作归属 ID 由宿主写入，插件不能伪造。排队/运行任务按真实插件归属取消，保留旧任务种类与历史。凭据读取改为独立 `extensions.credentials.v1` 能力，只有 Codex 票据模块声明所需范围。
- 构建集合以 `plugins/bundle.source.json` 为唯一来源，当前包含七个独立模块。七包签名/资源/锁清单及各模块独立进程验证已通过，开发集合位于构建目录的 `bundle-seven`。该集合中的 Cindy/提示词包早于后续变更；最新提示词程序另有完整分块传输证明。正式发布须从最终源码重建全部程序与资源。
- `plugins/cindy-provider` 已接管完整静态目录、精确别名、免费池与协议可用性、价格参考及结构化 401/403/budget 分类。宿主只传状态码和结构化错误字段，不传原始错误正文。环境变量仅用于首次迁移，持久插件配置是后续唯一来源；非法配置不会改变当前状态。
- Cindy 在请求开始前捕获价格快照，HTTP 与 WS 的异步账单沿用该快照。WS 每回合刷新配置，旧账单不被后续配置改变；插件停用后无法开始新请求，计数与 Embeddings 入口也受此限制。宿主保留金额计算、用量提交和已观测健康状态的持久化，不依赖插件存活完成旧账单。
- `plugins/image-tools` 已接管生成/编辑校验、模型投影、默认选项及每次上游请求一张图像的计划。图片、提示词正文和密钥不进入 RPC。Image Studio 与 Responses 图像桥接配置独立迁移，原有关闭状态不改变，Cindy 目录接收图像能力快照而不持有第二套开关。
- 图像任务绑定插件执行上下文；停用、重新配置或替换时取消宿主正在执行的任务，排队任务不会继续发请求。服务在插件配置加载后启动，后续启用可由新任务启动；图像关闭时不启动清理。持久任务记录、已保存图片和输入保留。
- manifest 可用 `config_flag` 控制贡献。普通用户仅收到公开表面的元数据，管理操作和配置字段不对外暴露。Image Studio 入口跟随贡献启停，插件故障保留输入与不可用原因；进度、取消及已有结果接口继续按宿主用户身份授权。
- Image Studio 完整页面、API 适配及交互用例已迁入 image-tools；普通用户的资源和 UI 会话只授予用户权限，管理员打开用户页面也不升级权限。原 IndexedDB 仍由宿主按当前登录用户存取，Blob 原样通过具名本地资源传递；旧无归属历史保留在隔离 store，不能被新登录用户读取。已有任务、取消、下载与历史使用宿主保留资源，插件故障时继续可用，新建/重试仍要求对应能力。
- Cindy Responses 图像桥接的默认模型、模型映射及控件决策已迁入 image-tools；Provider 只提供目录与能力事实。宿主只传有界模型/控件事实并校验返回计划的索引和计费身份，不传正文、图片、密文或凭据；原始 JSON 与协议 IO 留在宿主。实际账号范围在缓存和执行上下文建立前复核。关闭桥接后不再绕过校验恢复旧出站行为；普通文本不依赖图像插件，官方原生图像路径保留。
- Cindy 原生 Images 的生成/编辑控件校验也已迁入 image-tools，选择账号后及实际转发前执行相同能力检查；关闭目录不再跳过 Cindy 的验证。普通 OpenAI 原生图像流程继续属于宿主。上游 IO 脱离客户端取消时仍保留独立的插件执行期限，插件停用/重配/更新可以取消其工作；已观测用量的账单上下文继续独立提交。
- Cindy Alpha Search 的模型准入、Responses工具能力、helper-qualified Messages回退和每次最大搜索次数已迁入 Provider。宿主仅传公开模型与账号作用范围，仍保留认证、正文、取消、代理、上下文、安全证据、健康/failover、计费和用量。完整响应缺少搜索证据时仍可在客户端输出前按独立事实回退；能力缺失与缺少证据分别建模，其他错误不会换协议。
- `plugins/admin-observability` 接管账号流量分类规则和指标展示。宿主只保存有界计数器，并在请求开始时冻结插件提供的规则表；插件停用后旧请求仍正确结算观测，新请求不再开始计数。指标读取有独立能力，不取得账号凭据；宿主还会复核实际仍启用的凭据能力。
- 主题字体、配色、圆角、阴影和动画参数已进入签名插件资源；宿主保留中性默认值和可访问性规则。公开资源仅允许清单声明的 CSS/WOFF2，并绑定整份资源清单摘要；配置页面和脚本不公开。插件停用或故障时撤下样式，首次加载失败不无限阻塞应用。
- 主题迁移恢复可证明匹配的上游圆角/阴影类，由插件参数决定外观。对本批 212 个 Vue 文件的静态比较确认模板结构未变，业务脚本仅 App 的主题刷新接入发生变化；仍有混合文件和写死的局部主题声明待审查，不把机械替换算成全前端审查完成。
- 补回25个文件中34处与上游唯一对应的圆角类，另将验证码、分页和自定义内容的明确圆角/颜色接到中性默认与插件参数。账号、仪表盘、Ops及通道图表的下游配色已移入插件CSS，宿主通过通用颜色读取器监听主题加载、撤下及深浅色变化，现有图表可即时更新；图表数据、统计和交互算法未改变。滚动条和未知健康标记也使用可撤销样式参数，混合文件仍须完整审查。
- 保存相同的规范化插件配置不再取消进行中的宿主任务；实际变更和停用仍取消，定向回归通过。插件开发说明已更新为现有 SDK、Bridge 和实际打包入口。
- Cindy 显式余额复查的模型、最小请求内容、两阶段判定及受管通道模型投影已进入 Provider 插件。宿主继续执行有凭据的请求、租约和账本事务；插件停用/配置变更取消任务，停止新建、恢复和领取，历史读取/暂停/取消接口保留。自动删除旧诊断账本的路径已移除；过大响应不能被截成“成功前缀”误判恢复。余额复查入口跟随贡献及配置，故障保留输入并阻止新请求。
- Cindy 余额复查、重复身份清单和分组审查的完整界面已进入 Provider 包；宿主原组件只挂载独立界面。表格的最近巡检通过 `display_fields` 声明，由通用渲染器显示，文案/状态颜色随插件更新。分组 Key 选择使用新的脱敏只读资源，不向插件发送完整 Key。复查的嵌套选择在宿主校验；“全部/筛选”只能使用宿主声明且实际强制的 Cindy/API Key 范围，不能把部分账号范围扩大为全库。
- Cindy 重复身份归组和建议归属、分组输入及拆分两侧决策已迁入 Provider。凭据归一/摘要、显式 Key 选择保护、成员指纹、原有配置复制及原子关系写入仍在宿主。插件只收到身份摘要和状态事实。分组 SQL 原来仍识别旧 OpenAI+Laxa 行，现改为已迁移的 Cindy/wire/profile 身份；旧投影不再被当作 Cindy。拆分后的 Cindy 一侧取得正确平台身份，目录审查包含已迁移分组，保留另一侧、倍率、成员优先级及未选择 Key；指纹纳入插件拆分计划。
- Cindy的Codex清单描述、推理文案、截断方式和自动压缩阈值已进入Provider的独立展示计划。宿主保留原生ModelInfo格式、基础指令、排序、普通模型合并及来源/ETag隔离；校验展示计划不能改变模型身份、受保护容量或已声明输入/推理能力。普通GPT/Grok描述及DeepSeek默认可服务模型列表已确认属于官方0.2.7基线，不因整文件含下游改动而迁出。
- Codex 配置拒绝 null、尾随 JSON、未知字段和重复模型，校验失败不取消当前票据执行周期。远程技能候选组装沿用调用者上下文，文件策略阶段不再丢失同步取消信号。
- Codex 每账号客户端身份的派生、schema 校验、UA 拼装、沙箱判定及下游默认指纹模式已进入独立 `profile` 包，经具名 RPC 调用；宿主保留数据读取/保护、同账号版本条件写入和协议 IO。启用策略的单次身份计算失败会阻止 HTTP、WS、连接测试、模型查询及刷新出站，不仅检查进程是否存活。关闭插件退回上游身份默认，已有身份数据在普通编辑中仍保留；上游显式指纹选项和租户/凭据隔离继续属于核心正确性规则。
- 身份回填改为在插件初始化后启动，执行上下文随插件取消；元数据提交复用宿主事务、受保护字段与调度快照路径，并以输入账号版本拒绝迟到结果。诊断来源按真正选择的指纹报告，修正强制 Codex 模式及重建旧版本 UA 时的错误标签。插件目录的 include_inactive 现传到数据库查询，停用/错误账号不再被 active-only 查询提前排除。
- 通用请求扩展的 header 投影不能覆盖宿主认证、账号路由、租户会话和 HTTP framing；292 票据是明确的逐请求例外。非法值及大小写重复项拒绝，所有插件成功后才提交 header 变更。
- Codex 请求压缩开关与适用端点规则迁入插件；首次迁移继承原环境值，运行时不再读宿主配置或第二份全局原子开关。普通转发、额度探针和连接测试共用一个传输计划入口；插件只收请求元数据，正文压缩、完整读取与重放仍在宿主。运行设置可单独保存压缩开关，清除代理只停用票据。
- Codex 推理恢复已有独立 `extensions.recovery.v1` 能力，插件拥有精确拒绝码判断、待移除项选择和恢复/回放默认启停。宿主只传错误结构、项索引和类型计数，不传提示词、密文、凭据或完整错误；原始请求、同来源校验、一次预算、流式承诺和用量记录保留在核心。插件停用不再尝试恢复，故障记录不可用原因。请求相关选择不进入配置策略缓存。
- 正向 Chat 推理回放的可用字段、输出种类、工具数量及可省略 reasoning 规则已进入 Codex 插件。宿主校验受支持的协议子集，按冻结规则摘要隔离缓存；恢复识别、选项和项选择均携带实际账号范围及灰度。插件缺失时，签名拒绝仍由核心识别为请求级失败，不能误触发账号池重试或健康惩罚。
- 续接诊断解释规则和独立页面已进入 Codex 包，现有错误详情通过通用挂载显示。具名只读资源仅投影脱敏结构事实，按已存事件中的真实账号与当前能力/灰度过滤；完整错误、名称、请求正文和凭据不进入界面。宿主继续保存协议事实与通用原始诊断查看，插件故障不改变请求重试判断。
- 提示词审计管理页面、节点编辑、策略/运行概览、事件详情及删除弹窗已进入 admin-observability 独立包。旧路径仅挂载，原菜单、分组/风险控制权限和删除确认票据保留；界面通过具名资源沿用原授权/审计链，已存节点密钥不回显。安全审计的实际强制检查仍由宿主执行，此次只迁移计划指定的管理界面。全局管理贡献新增全账号声明，显示、资源描述与服务端执行均要求通配范围和100%灰度，不能用部分绑定管理全局配置。公开分页组件接收宿主提供的选项，沿用非敏感浏览器分页偏好。
- 界面贡献可声明自身能力；能力关闭时撤下对应入口，其他仍启用能力的入口保留。恢复表单和 Cindy 复查已接入，故障时原值保留且不允许提交新的配置编辑。
- 插件升级增加逐能力启停/灰度快照，迁移期间实际绑定全部关闭，完成后原样恢复。重启、用更新的包接替未完成升级、迁移中管理员主动停用均保留意图；已有插件新增能力默认停用。旧有通用账号操作结果沿用已记录的24小时TTL，临时加密上传内容的既有过期规则不变；它们与持久票据/诊断账本分开管理。

## 可复用验证

- Cindy展示计划独立复制/截断/压缩用例，以及原Rust ModelInfo完整字段契约、普通模型合并保留和恶意身份/容量/基础指令/推理计划拒绝三项宿主定向用例通过。最初编译被并行搜索改动中的转义错误阻断，修正后实际执行；没有真实模型请求。
- 发布/回滚、持久修订号和编排防替换沿用Luna代理的定向通过结果，来源见根 `artifacts/evidence/v027-prompt-boundary-agent-review.json`。主代理复核后额外收紧未注册URL和非哈希路径，验证来源权限、路径拒绝、固定来源合成下载、当前/73文件历史候选读写及原子发布通过；追加profile上限在任何下载前拒绝两种超限的验证通过。旧目录布局助手只移至测试，未重跑无影响的用例。
- 35个界面文件的完整源码与官方基线在静态配色类之外逐字一致；动态绑定、事件、脚本、文本及scoped样式均未变。73处明确中性调色/对比度调整已绑定当前摘要并关闭审查，证据在根 `artifacts/evidence/v027-presentation-source-review.json`，其余混合文件不自动关闭。
- 局部主题追加验证：34处圆角修改均只替换明确上游半径类，25文件当前摘要全部匹配，记录在根 `artifacts/evidence/v027-neutral-radius-migration.json`。实际Vue颜色读取器在合成浏览器验证浅色默认→插件→深色插件→撤下插件、非法颜色拒绝及卸载清理，页面错误和外部请求为0，证据在 `artifacts/evidence/v027-chart-colors`。前端类型、修改的颜色调用点ESLint、插件CSS语法与核心CSS编译通过；未重跑业务用例。首次夹具无法从pnpm顶层找到esbuild，改从已安装Vite依赖解析后通过；首轮类型检查发现一个被替代的unused深色变量，移除后仅复验类型与该文件。
- 提示词审计迁移的原交互/组件/数据转换与新具名资源、宿主入口共30例通过，前端/SDK类型、显式选定文件ESLint和独立构建通过。全局贡献、资源可用性与实际调用范围一致性及普通用户/未登录拒绝通过；原插件资源持久停用回归复用/通过。真实隔离iframe在1200/390px与深浅色四场景验证配置修订号、节点密钥空值/显式替换、故障输入保留、关闭及无外部请求。首次浏览器检查发现固定底栏在iframe视口外，改为页内吸附底栏后复验通过；DOM与截图在根 `artifacts/evidence/v027-plugin-prompt-audit-ui`，未作目视结论。后续仅还原公开分页组件的上游中性边角类，未重跑业务用例。
- Codex 回放规则缓存隔离、实际账号/灰度、插件缺失时请求级拒绝及原正向回放定向用例通过；旧 recorder 夹具补入规则后只复验该失败项。诊断投影验证敏感字段去除、真实账号范围、灰度与旧代次拒绝；错误详情原用例及只传记录 ID 的断言、前端类型与修改组件 ESLint 通过。离线浏览器900/390px、深浅色四场景验证解释、故障、空记录和切换，页面错误/外部请求为0。截图与DOM证据在根 `artifacts/evidence/v027-plugin-codex-diagnostics-ui`；图像工具不支持读图，目视检查仍未完成。
- 覆盖清单导入已审记录时逐条验证上游基线、角色、当前摘要和指纹，错误/重复/元数据声明在写入前拒绝；隔离Git夹具验证导入、变更失效、拒绝时不写入及未完成门禁。已纳入541条生成物/素材/锁文件/内置来源对应证据，仅证明所声明来源和字节一致，不声称依赖漏洞扫描或全部内置材料语义审查。
- 提示词生命周期四项定向用例通过：停用/故障停止两个服务并保留数据、重新启用恢复、stopped sync不创建任务、健康启动只Reload一次及Stop/Start可恢复；未重跑已通过业务测试。这批由用户授权的Luna/max代理实现，主代理复核了生命周期和失败收尾，源记录在根 `artifacts/evidence/v027-prompt-boundary-agent-review.json`。
- 导入原十项预览、明确目标组、凭据与设备冲突优先级、事务错误脱敏用例通过；独立策略优先级和非法阶段用例通过。1002项合成请求覆盖跨块冲突优先级、RPC负载上限、拒绝插件选择其他账号及停用无回退；旧任务生命周期和正常完成/停用取消两例通过。没有重新运行未改动的前端或真实模型调用。
- 账号任务的宿主执行租约、旧代次拒绝、重试归属/当前版本/收窄范围、管理动作实际账号范围通过；原普通任务重试、资源关闭与提示词领域取消定向回归复用通过结果。本地 PostgreSQL 验证更新等待宿主租约、更新中及更新后的迟到任务创建拒绝，原停用取消同事务用例通过。新测试最初缺少 unit 构建标签；旧 PostgreSQL 夹具缺少必需包字节，补齐后只复验失败项。Linux race 的对应五例仍等待正常 CI，本地未运行。
- 导入界面原六项交互、宿主/公开SDK/插件 UI 类型与修改组件 ESLint 通过。实际 Blob Worker 在1200/390px、深浅色四场景完成合成文件解析、预览和任务提交，页面错误与外部请求为0；通用高度修正直接影响的分类管理桌面/窄屏两例通过，未重跑其余已通过 UI。证据在根 `artifacts/evidence/v027-plugin-import-ui` 和 `v027-plugin-dialog-sizing`，未作目视结论。宿主任务归属及阻止插件替换用例通过，首次夹具缺少必需幂等键，补齐后仅复验该例。
- 原生图像独立控件一例、宿主敏感内容隔离、不可用免费池、普通 OpenAI 流式/非流式断线收尾与上下文契约、HTTP 拒绝路径共七项定向用例通过。插件独立取消期限、配置变化同步取消及相同配置不取消三项通过；第一次修改使用异步回调使两项即时取消断言失败，已恢复同步取消语义后复验。当前 Windows 的 CGO 为0且无 gcc/clang，未运行本地 race；对应 Linux race 检查已加入正常 CI，尚未执行。
- Cindy 管理策略的独立所有者/终态及两侧选择两例、宿主凭据不出 RPC 与停用拒绝一例、原重复项/规范化/分组服务和仓库定向回归通过。本地 PostgreSQL 已验证 canonical Cindy 的预览漂移、完整事务回滚，以及原组保留 Cindy、旧 OpenAI+Laxa 归普通侧；后者覆盖停用成员的结构身份。旧集成夹具使用迁移前平台，不能证明当前身份条件；已修正相关夹具。未做线上账号操作或模型请求。
- Cindy 三个独立界面九项原交互、通用巡检显示三项及账号页对应布局一项通过。完整宿主与 SDK/插件 UI 类型检查、修改组件 ESLint 通过；宿主初次类型检查发现 ES2020 不支持 `Object.hasOwn`，已改为现有兼容写法。后端 Key 脱敏、嵌套选择与固定范围/灰度四项定向用例通过，路由包编译通过。开发签名 Cindy 包包含编译界面与显示声明，并通过签名/资源校验。
- Cindy 离线浏览器覆盖三个界面各1200/390px与深浅色，共十二场景，页面错误和外部请求均为0。复查与重复清单八场景通过后，分组故障断言由 `disabled` 改为实际的祖先 `inert` 契约，仅补跑分组四场景；首次夹具上下文事件名拼错已修正。验证选中目标、脱敏Key、故障时输入保留和已有任务可暂停。截图及分组结果在根 `artifacts/evidence/v027-plugin-cindy-ui`，未作目视检查。
- 图像 UI 九项、插件 API 三项、原历史存储四项、新用户隔离和旧历史保留各一项及保留控件回归通过；后端用户页面角色、保留资源和四项图像所有权/脱敏用例通过。前端类型和修改文件静态检查通过。离线 iframe 在1200/390px、深浅色下验证 Blob 往返、历史保存、故障历史可用及用户切换隔离，页面错误与外部请求均为0；证据在根 `artifacts/evidence/v027-plugin-image-ui`。图像工具不能读取截图，未声称目视检查。
- Responses 桥接独立策略、宿主协议/映射、精确 JSON 数字、敏感数据不进入 RPC、账号范围/缓存/执行上下文、计费身份/工具索引及停用不出站的定向用例通过。原数字兼容用例因宿主函数移除首次无法编译，迁移到宿主事实提取与插件控件校验后通过。Image Studio 页面已进入开发签名包并通过签名/资源校验；独立进程的 Studio 及桥接 RPC 通过，首次命令缺少 Windows 的 `.exe` 后缀，修正路径后只复验该用例。正式七包仍须从最终源码生成。
- 独立更新基础接口与迁移 `250_plugin_independent_updates.sql` 已实现：整包摘要、安装修订号、执行代次、持久候选及固定/内置版本策略。更新候选仅校验，不获得宿主能力；旧实例通过会话锁排空，只有目标插件被替换。配置保存已改为数据库 CAS 成功后才应用到进程。开发与接口说明见 `docs/PLUGIN_DEVELOPMENT.md`；业务界面按上文实现保留，正式制品尚待本次发布。
- 本地 PostgreSQL 已验证更新等待全部旧进程、配置并发冲突、中断后续作、停用意图、旧代次状态与租约写入拒绝；原三项 bundle 意图回归通过。新增服务用例验证错误签名、版本冲突、相同包幂等及保存冲突不取消工作；原配置规范化回归通过。界面页四例、类型与修改文件 ESLint 通过；资源会话跨实例及版本绑定用例通过。首次 PostgreSQL 命令因测试实例已退出而未执行，使用原数据目录恢复后补跑通过，数据未重置。

以下均在本地合成数据或本地 PostgreSQL 完成，未调用真实模型、未访问生产做验收。

- 前端类型检查及全部本次修改 Vue/TS 文件的 ESLint 通过。账号选择/批量菜单/单账号菜单 20 例通过；插件槽与插件页 4 例通过；全局设置改走插件的定向保存测试通过。
- 宿主插件接管：无旧票据回退、RPC 故障阻止、脱敏摘要、Compact 最终模型、两种调度器与停用凭据刷新边界通过。真实协议进程与签名包前序曾通过；最新二进制需在发布前由正常 CI 构建验证，不能沿用旧构建物。
- 新增插件取消、主体/模型/账号/有效期校验及中断恢复用例通过。原状态机、私有代理证书固定与假上游唯一请求用例沿用前序结果；正常 CI 将覆盖最终模块。
- 额度真实周期、多窗口/未知期限、独立时钟、撤销窗口、旧错误恢复及不污染普通冷却通过；修改所影响的快照写入、Spark 转换和 OpenAI 提前暂停用例通过。
- 两个原 429 用例断言的是已废弃行为，已改为验证独立额度状态和未知期限；仅重跑失败用例并通过，其他瞬时 429 用例及 server 清理编译/测试已通过。
- 推理强度：已有映射/zstd 出站证明保留；新增不支持协议拒绝、OpenCode Go 两种最终出站载荷及批量选择持久化用例通过。
- 真实本地 PostgreSQL：新并发快照排序/旧窗口时钟、费用历史缩减重新校准通过。前序的插件 CAS/租约/停用取消同事务、bundle 重入/停止状态迁移、费用基线持久化结果保留。
- Cindy：带 `unit` 标记的精确 budget/HTTP401/403 分类实际执行通过；原有定价测试覆盖长上下文、缓存档、优先级价格、缺价拒绝、别名与分组价格优先级；新异步快照、WS 回合更新和辅助入口停用用例通过。`TestCindyProviderExtensionRuntimeUsesIndependentProcess` 已验证真实独立程序返回价格快照。
- 图像：独立策略用例、宿主校验、停用取消、四工作线程、已移除模型拒绝、八个图像 HTTP 适配/授权用例通过；原有关闭默认不变。前端图像页/路由/侧栏/通用表面 30 例通过，类型检查与本批修改的 ESLint 通过。
- 观测：原有 HTTP/WS 结果分类、冻结规则收尾、指标读取范围、凭据权限撤销、实际独立进程经 broker 读取合成计数器均通过。新的公开主题资源测试覆盖声明、启停、摘要和篡改拒绝；前端样式加载/撤下与错误 URL 拒绝两例通过。
- 主题：新增加载代码的类型检查、ESLint 和一次 Vite 生产构建通过；后续圆角/阴影修正仅影响样式，当前 CSS 已单独重新编译。`plugins/admin-observability/scripts/check-theme.py` 使用本地 CSS 和被拦截的合成页面，900px/390px、深浅色、停用→启用→停用共12个状态通过，页面错误0、外部请求0。证据在项目根 `artifacts/evidence/v027-plugin-theme/theme-check.json`，截图只作留存，未声称逐像素审阅。
- 提示词管理追加验证：编排、受管模板保护、原文根地址改写、初始化保留历史、停用后恢复与故障区分、迟到来源丢弃通过。目录清单、路径/引用图、历史73文件候选与458文件种子并存/发布/回滚、文件系统保留旧目录通过。真实独立程序分块传输全部文件并逐个核对摘要，累计超过RPC上限；GitHub来源测试只用模拟HTTP。数据库模拟事务证明来源信息完整且不替换当前运行版本，公开GET/HEAD/cache边界、前端11例、类型与定向ESLint通过。未下载线上来源、未调用模型。
- 提示词作用范围：全局管理与初始化按实际启用绑定选择唯一领域提供者，逐账号请求仍严格按目标作用范围分派；不再把只绑定 OAuth 的插件误判为停用。五个定向用例覆盖范围隔离、歧义提供者拒绝、停用/故障及来源取消。
- 技能同步恢复：整个任务以数据库创建时间为起点限制十分钟；来源自身的五分钟限制保留。启动与状态读取使过期未完成任务结束为失败，不自动重放；阶段更新和候选提交由数据库实时时钟再次校验，保留其他实例未过期任务和已完成历史。本地 PostgreSQL 连接私有临时表验证过期、存活、成功和迟到提交，未改共享数据；服务过期不再取源及原成功/失败流程、HTTP 创建用例通过。
- Cindy 追加验证：独立模块的复查策略及通道规则通过；宿主十一项定向复查/协议/取消用例通过，404/503 区分与停用拒绝用例通过，两个通道适配用例通过。余额面板五例、前端类型及三文件 ESLint 通过。保留旧记录的数据库集成用例已替换原删除断言，本次未执行该集成用例；删除入口已从服务和仓库接口移除。新 Cindy 源码尚未进入正式签名包，最终构建仍由正常 CI 完成。
- Codex 身份追加验证：独立 profile 策略、十五个宿主身份/刷新/HTTP 压缩边界用例、七个默认/显式指纹及数据保留用例、include_inactive 查询用例通过；本地 PostgreSQL 的账号版本条件写入通过。三个 header 投影/正常票据回归通过。Wire 装配已重新生成；旧 cleanup 测试漏掉前序新增的 PromptDomain 参数已补齐，该定向关闭用例通过。没有发送真实模型请求。
- Codex 传输追加验证：独立插件的压缩适用范围及票据开关独立性通过；八个宿主协议、完整读取、映射、推理强度、普通/compact/图像连接测试及搜索出站用例通过。390px 深色离线浏览器验证压缩开关保存/关闭、清代理不改变压缩、原控件底部对齐和无横向溢出，页面错误0；截图为根项目 `artifacts/evidence/v027-plugin-ui/transport-setting.png`。图像工具未提供可读图片输入，未声称目视像素验证。
- 恢复策略追加验证：原八组严格拒绝码、工具关联、保留非密文字段、同来源重试、不可滑动记忆、HTTP/透传/SSE用量及已提交输出不重放回归通过。独立策略、RPC不接收正文/凭据及停用不增加请求通过。恢复字段三例和创建/编辑/批量表单十三例通过，前端类型及相关ESLint通过。能力级贡献与原正常票据投影测试通过。
- 本地 PostgreSQL 验证逐能力升级意图及灰度比例、迁移暂停期间拒绝提前启动、重启及换包续作、明确停用和原卸载意图保留；正常插件停用取消所属任务的事务回归通过。新迁移仅增加 bootstrap 意图列，不删除线上记录。
- 离线浏览器脚本：`plugins/codex-runtime/scripts/check-ui.py`。900px/390px、深浅色、HTTP405 反馈、清除并关闭、批量四目标验证通过，页面错误0；DOM 边界验证对齐且无横向溢出。截图位于项目根 `artifacts/evidence/v027-plugin-ui`；当前模型工具未提供可读图像输入，未声称完成逐像素视觉复核。
- `git diff --check` 最近通过。后续修改仍需最终静态检查。
- Cindy 搜索计划与目录使用同一真实账号作用范围、同一插件及相同上游模型；额外 JSON 与过长身份拒绝。定价缓存复用前同样核验账号灰度，已捕获的异步账单引用保留。对应五个定向用例通过，完整四文件审查见根 `artifacts/evidence/v027-cindy-policy-scope-review.json`。
- 导入跨块身份计数由宿主不可变事实计算，prepare/finalize 跟踪标志必须一致；重复身份和错误设备归属不能被插件变成可执行计划，业务错误码优先级仍由插件决定。旧批量 ID 中任意非正值整体拒绝，合法去重/排序保留。八个精确定向用例实际运行通过，证据见根 `artifacts/evidence/v027-astra-import-integrity-review.json`。
- 管理员无贡献 ID 的配置页在停用/故障时仍能打开，不授予业务执行；业务页按能力绑定准入，AllAccounts 要求通配平台/类型及100%灰度。静态令牌不因配置修订或执行代次变化失效，操作仍走实时门禁。原选择器漏掉新测试名已纠正，四个新增顶层测试及子例实际 RUN/PASS，见根 `artifacts/evidence/v027-ui-session-admission-agent-review.json`；这不代表完整管理器已审完。
- 更新暂存与续作在候选验证前及提交前重新读取持久注册表，检查目标修订/配置/包代次、依赖及操作冲突，保留最新停用意图和新能力默认关闭。十个准入叶子用例通过；另修正旧续作夹具缺更新接口导致的假阳性，现确实进入候选启动并在失败时阻止提交。见根 `artifacts/evidence/v027-astra-update-admission-review.json`。该预检不代替下述仓库事务的最终图校验，也不是成功健康验证；完整混合调用链仍须继续审查。
- 旧上传接口现只用于首次安装，已有身份返回 `409 / PLUGIN_ALREADY_INSTALLED` 并要求专用更新；仓库并发冲突也不覆盖旧包。成功晋升后重新读取全部注册表，再检查并发布，不沿用其他插件的排空前快照。真实 PostgreSQL 的旧租约覆盖反例修复后通过，正常首次安装、冲突时旧包/配置/灰度/pending 保留、明确卸载后新 ID 安装和诊断账本保留均已定向验证；见根 `artifacts/evidence/v027-astra-update-persistence-review.json`。
- 未完成内置初始化不能再被独立更新插入并覆盖：Prepare/Complete 保留 `updating` 与固定版所有权，Stage 不把迁移期临时关闭绑定当成管理员意图。迟到 Complete 仅在持久化确认已被其他操作接手时跳过；普通失败仍报错。两条 PostgreSQL 反例及11个直接叶子用例已通过，见根 `artifacts/evidence/v027-astra-bootstrap-update-review.json`。最终读取后的并发写入、运行会话丢失和已完成bundle固定版竞争分别由下述独立证据限定，不以本组初始化结果代替。
- 分类赋值回传必须保留原文件夹和完整标签集合。全局文件夹/标签删除要求通配账号类型、平台和100%灰度，SDK新增只读具名资源可用性查询；独立页面显示、点击前复核及后端执行共用同一判断，其余分类操作不扩大为全局权限。8个新增定向用例及两份类型检查通过；根 `artifacts/evidence/v027-astra-account-taxonomy-review.json` 区分完整与局部审查。
- 账号型宿主 broker 每次调用读取一个当前安装快照，检查已启用状态、包/执行代次/已应用配置及真实账号灰度；列表逐账号过滤，凭据、指标和调度投影不能绕过范围。已发出的归属观测在停用后仍可按原账本收尾。请求头注入与本地调度也改为同一真实账号灰度。合成反例先失败后通过，认证/租户/HTTP帧头保护及具名操作隔离回归通过；见根 `artifacts/evidence/v027-broker-and-request-scope-review.json`。这不代表全部调用方或跨实例生命周期已审完。
- 模型目录查询保留真实 AccountID，0/50/100%灰度与账号缓存隔离已验证；读取缓存前只点查选中插件，并核对已应用配置、策略取消与配置修订。候选上限对齐宿主已有512字节，长命名空间后续短叶子正常匹配，513仍拒绝；固定93条目录与迁移前宿主表一致，价格未变。六个精确定向用例通过，见根 `artifacts/evidence/v027-astra-model-policy-final-review.json`。
- Codex真实账号的身份、压缩、指纹默认值、sandbox与诊断均保留ID及OAuth/Setup Token类型；后台回填逐适用账号获取租约并条件写入，不先要求全局插件可用。创建前和共享策略保留无账号语义，显式用户指纹配置和原字段不改。六个新增目标方法及两个直接旧契约通过，见根 `artifacts/evidence/v027-astra-codex-scope-review.json`；完整指纹/gateway/OAuth大文件的未审部分仍保留待审查。
- 宿主业务IO租约现在与普通进程生命周期租约区分：业务准入核对当前enabled、revision/config/包/执行代次，并先核对运行时已应用配置；停用插件的临时配置诊断仍允许普通process lease。两项真实PG、两项完整接口内存反例及直接影响的后台回填用例通过，见根 `artifacts/evidence/v027-astra-host-io-admission-review.json`。这不等于异常丢失数据库会话后在途IO已经被撤销。
- 官方自动用卡服务仍由原provider启动，插件迁移曾漏掉它的关闭调用；已仅补回Stop并重新生成Wire，实际worker在合成仓库阻塞的反例先失败后通过。Make关键测试列表的旧Cindy路径也已改为插件内的同一测试，22个静态目标均存在；不因路径修正重跑业务测试。见根 `artifacts/evidence/v027-lifecycle-wiring-review.json`。
- 提示词实际请求带AccountID，命中请求缓存也重新检查当前账号准入；内容/编译快照仍按同一请求或WS回合冻结。跨账号、撤销、协议转换时只按确切载体来源恢复本次注入，不搜索删除客户同文system；固定两种载体证明、最多64KiB原始instructions且只有一份完整缓存输出，来源不确定拒绝。来源策略证据见根 `artifacts/evidence/v027-astra-prompt-request-scope-review.json`。WS已改为只保留当前回合提示词缓存，四个直接回归通过；响应转发和AfterTurn结束后才替换缓存，不动独立的重放历史或异步账单，见根 `artifacts/evidence/v027-prompt-turn-cache-review.json`；这不等于连接总内存已有上界证明。
- 自动内置更新在启动后读到晚到pinned/updating意图即停止stage，状态冲突或目标移除须重读持久意图确认。completed分支只清理本次唯一候选，不清旧包或Prepare已接管路径；真实bootstrap/installer离线反例及原completion直接回归通过。见根 `artifacts/evidence/v027-astra-bundle-pin-race-review.json`，先前completed分支的固定版竞争已关闭。
- 发布源码固定为main准入的完整SHA，发布前检查Release/draft/版本镜像是否已存在或不确定；构建/load与push分离，显式draft附齐包和lock的摘要后再公开，失败保留现场不覆盖。签名身份初始化完整分页检查Secret名称；checkout不保留token，构建不继承显式发布token，私钥仍仅签名步骤注入。14个Python和3个Go定向方法、YAML/run块语法通过，见根 `artifacts/evidence/v027-astra-release-supply-chain-review.json`；未真正签名、发布或核对远端不可变规则。
- 七包29项贡献已逐项声明能力；有管理动作的贡献同时要求Admin与声明的业务能力，宿主投影平台/类型/灰度交集，前端按同一uint64账号分桶检查完整选择集。字段保留草稿而有效执行值独立计算；普通连接测试在插件选项不可用时仍可用原生默认，显式非空越范围API选项拒绝。全局主题要求通配UI绑定和100%。配置尚未应用时不借旧true flag准入管理动作；原任务历史仍可查看。定向Go/UI及类型验证通过，详见根 `artifacts/evidence/v027-astra-contribution-admission-review.json`。
- 运行租约新增可观察会话接口：一秒间隔、最多一秒Ping探测，同一goroutine串行Ping/Close，无自动重连；检测失联后运行时排空并终止，宿主策略取消可传播到已脱离客户端的上游IO，计费收尾仍独立。21个定向叶子执行通过。真实本地PG明确复现“新代次提交早于失联通知”，因此保证仅为检测后本地撤权，不声称撤销已到上游的请求或全系统exactly-once；持久任务租约、IO前spent CAS及旧代次写入拒绝继续防重复。见根 `artifacts/evidence/v027-astra-runtime-lease-loss-review.json`。
- 注册表六类图写入已在SERIALIZABLE事务内读取并校验最终依赖/操作所有者，冲突回滚同事务修改；不把旧版本或直接SQL写入器视为受保护。管理界面保存读时revision/package，配置回执来自本次条件写入；旧草稿不自动变基，相同配置CAS不取消工作。证据集中在根 `artifacts/evidence/v027-registry-precondition-integration-review.json`，公开接口与限制见插件开发手册。
- 遥测Begin/Snapshot按实际账号范围选择策略；今日统计只在确认日志持久化后失效，generation阻止旧在途读回填。密钥显示相关响应禁止缓存，既有管理员与专用密码规则不变，见根 `artifacts/evidence/v027-astra-admin-adapter-review.json`。Cindy余额探测和原生Messages也已补实际账号门禁，共享目录查询保持原语义，见根 `artifacts/evidence/v027-astra-cindy-catalog-completion-review.json`。
- 历史利润配置不能重启已退役的运行时门，旧列、倍率和定价上下文保留；插件弹窗恢复移除inert而不换输入节点；导入取消/非JSON选择保留草稿，accepted新JSON失败不回退旧数据。对应证据为根 `artifacts/evidence/v027-dormant-retirement-import-review.json`、`v027-ui-host-mechanism-review.json`、`v027-import-draft-coverage-review.json`。SDK请求超时还会释放取消监听器，详见 `v027-public-sdk-contract-review.json`。

## 交付状态与后置审查

- 按用户决定，DeepSeek的节假日/峰谷默认遵循已整合的Sub2官方 `v0.2.7` 精确提交 `aea725f2ea644d5592d0bbb1d63b607efa7e200a`：`billing_service.go` 的 `deepseekPeakMultiplierAt` 只区分北京时间工作日指定窗口与周末，没有年度节假日日历；当前窗口与其等价，不新增日历。`4167b92de` 实为下游身份/价格修复，不能作为官方来源，也不因本次节假日决定撤销该修复。出处和官方Pro价格逻辑与下游修复的独立差异见根 `artifacts/evidence/v027-sub2-upstream-pricing-factcheck.json`；本次未改计费代码或生产。
- 七域均已有实际独立实现；混合职责中尚未完成的全文审查保留在覆盖清单，不能以七包可构建冒充全文审查通过。没有具体发布影响的剩余审查后置，不再要求清单归零。
- 全下游差异清单已由 `.downstream/refresh-coverage.mjs` 刷新，包含逐文件角色和内容摘要；分类不会自动标记已审查，修改字节会使旧结论失效。根项目维护通道、清理与 Worker 的已修正边界和验证记录统一见项目根 `artifacts/evidence/v027-root-review.json`，完整逐文件审查仍未完成。
- 普通/Spark额度、真实30天窗口、未知恢复时间、迟到账单和显式推理强度的最终调用链已静态复核，证据在根 `artifacts/evidence/v027-quota-agent-review.json`。首次已结算观测惰性建立费用/使用率基线，导入前消耗不进入增量估值；主体、倍率、窗口或活动代次变化隔离旧基线。既有通过用例直接复用，没有真实模型验收；本轮不新增30天提前暂停策略。
- 通用插件任务、配置、能力范围及多域依赖的其余全文审查保留 pending；已解决的事务准入、失联检测与 WS 提示词缓存问题不代表大文件其余区域已审完。历史维护/诊断 WIP 停止扩修，实际部署仅使用已审固定发布集合；正式构建与交付尚未进行。
- 当前仍是本地开发阶段，未创建 PR、未构建发布最终镜像、未部署。生产手册仍指向旧版本，必须等实际统一交付后更新。
- 提示词/技能完整管理页面及高级设置已迁入插件；模板编辑、发布/回滚、同步和冲突处理仍沿用原有体验，界面语言按上方最新确认规则处理。宿主页面仅挂载 `ExtensionPage`。账号分类导航、管理、批量分类、批量测试、详情分类编辑、导入界面和导入策略已迁入账号工具包，宿主保留轻量挂载适配和通用任务进度。其他账号增强及完整更新调用链仍需随其余域逐项核查。
- 单账号文本测试字段改为插件声明，由通用文本区域渲染；停用保留草稿并停止发送自定义文本，后端也校验对应能力。单个和独立批量界面继续共享同一用户的浏览器草稿，桥接只传同一用户/插件命名空间中的偏好。JSON 参数去除 Vue 代理，上传文件保留原生对象；表单事件通过 sandbox allow-forms，CSP 继续拒绝直接网络表单提交。
- 账号 UI 十二项交互/详情回归、四项通用字段、单账号测试七项、偏好/资源客户端四项及类型/定向静态检查通过。后端目标范围、灰度、筛选限制、上下文所有者、响应脱敏、停用自定义文本不出站和原文本协议定向用例通过。浏览器桌面导航与分类创建先通过，修正响应式数组克隆后只补跑剩余八种界面/尺寸场景，均无页面错误或外部请求；证据在根 `artifacts/evidence/v027-plugin-account-tools-ui`。图像不可读，未作目视结论。
- 公共 UI SDK 和具名数据资源接口已接入提示词、账号工具和图像页面，原 HTTP 认证/审计链保留，操作检查持久绑定和包身份。宿主与插件共享通用 UI 源码；插件编译边界拒绝导入宿主内部组件。角色限定页面会话、嵌套入口及普通用户 HTTP 入口已实现。
- 提示词 UI 十一个原交互用例、资源客户端与弹窗/接入八例、资源授权及页面角色/签名入口用例通过；宿主和插件 UI 类型检查、定向 ESLint、宿主 Vite 构建通过。四种尺寸/主题的实际隔离 iframe 浏览器验证通过，页面错误和外部请求为0，草稿保存与故障输入保留均验证。截图和 JSON 在根 `artifacts/evidence/v027-plugin-prompts-ui`；图像工具不能读图，未作目视结论。提示词包包含编译页面且通过资源校验，仍为本地开发签名。
- 正式发布者已配置到仓库 Secret，公钥在 SDK 的 `publisher.json`。独立发布工作流和正式镜像复用签名制品已实现，尚未执行 GitHub 发布或 OCI 构建。一个账号工具模块的独立构建、开发签名、清单/资源校验和实际进程通信通过；正式校验拒绝开发密钥，异平台资源验证用例通过。工作流 YAML、Shell 语法通过，正式签名仍须在发布任务中验证。
- 覆盖刷新器已支持带证据的决议、删除指纹和清单自身结构校验；总状态由有效记录计算。文本属性统一 LF，43 个 CRLF 文件转换后 Git 内容差异未增加。当前清单仍未逐项关闭，不能把工具的结构校验算作源码审查完成。复核撤销了153条证据不足或仍沿用修复前结论的关闭记录（123条仅测试符号清点、26条泛化文档头、4条旧发现）；原证据和有效测试结果保留，待补具体源码结论，记录在根 `artifacts/evidence/v027-review-quality-correction.json`。

## 续做入口与边界

- 本地 PostgreSQL 使用原目录 `artifacts/tmp/sub2api-v027-postgres`，监听 127.0.0.1:55437；专用测试 DSN 为 `postgresql://codex_test@127.0.0.1:55437/sub2api_test_v027?sslmode=disable&TimeZone=UTC`，仅定向测试设置 `SUB2API_TEST_POSTGRES_ONLY_DSN`，不连接生产。
- 临时实例退出时使用上述原数据目录恢复，不重置数据；前保活实例已退出，当前测试实例于2026-09-21经原目录恢复为 PostgreSQL PID 38748、端口55437，后续使用前核对运行事实。Astra定向测试各自创建的独立数据库已在确认零连接后删除，原 `sub2api_test_v027` 与数据目录保留；不要把测试服务当成卡住的命令终止。分类 CRUD/批量排序和插件停用取消事务的前序结果保留。
- PowerShell 的原生 rg 配置会影响结果，使用 `rg --no-config`；通配路径改为目录加 `-g`，不要向 Windows 传字面星号路径。
- 根项目不是 Git 仓库；源码 Git 在当前 worktree，主 checkout 和历史 worktree 不清理。
- `/admin/accounts/:id/usage` 可能触发模型探针；不要为了验收直接访问线上该接口。
- 内部未提交文件很多；不得重置或整体清空。旧测试的职责已迁到插件模块、宿主桥接及通用数据库测试，后续不能恢复双执行器来满足旧断言。
